<Page
    x:Class="Micacord.Views.AccountPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <Grid Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" x:Name="rootPanel">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
                <TextBlock Text="Micacord" 
                   FontWeight="Bold" 
                   FontSize="28" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,0"/>
                <TextBox Width="250" PlaceholderText="Email" Margin="0,0,0,2" Name="emailBox"/>
                <PasswordBox x:Name="passwordBox" 
                     PasswordChar="*" 
                     PlaceholderText="Password" 
                     Width="250"/>
                <Button Content="Login" Margin="0,10,0,0" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Center" Name="loginButton" Click="LoginButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
